## Packages
framer-motion | Smooth animations for page transitions and interactive elements
recharts | Visualizing attendance statistics (pie charts, bar charts)
date-fns | Formatting dates and times for attendance records
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["'Inter'", "sans-serif"],
  display: ["'Poppins'", "sans-serif"],
}

Colors:
Primary: #006837 (De La Salle Green)
Background: #F9FAFB (Light Gray)
Surface: #FFFFFF (White)

Auth:
Role-based access control required for routes.
Login expects username, password, and role.
